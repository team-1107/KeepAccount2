<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keepaccount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeepAccount">
        <activity
            android:name=".TimeSet"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
        <activity
            android:name=".MedalActivity"
            android:exported="true"
            android:label="@string/MyMedal"/>
        <activity
            android:name=".ChartActivity"
            android:exported="true"
            android:label="@string/MyGraph"/>
        <activity
            android:name=".SelectDateActivity"
            android:exported="true"
            android:label="@string/selectDate"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>